@model List<Member>
@{
    ViewData["Title"] = "Index";
}

<h1 class="display-3">Member</h1>

<a asp-action="Create">New Member</a>
<table class="table table-striped table-dark">
    <thead>
        <tr>
            <th>MemberID</th>
            <th>Member Name</th>
            <th>Member Surname</th>
            
        </tr>
    </thead>
    <tbody>
        @foreach (var item in Model)

        {
            <tr>
                <td>@item.MemberId</td>
                <td>@item.MemberName</td>
                <td>@item.MemberSurname</td>
                
                <td>
                <td>
                    <a class="btn btn-sm btn-info" asp-action="Details" asp-route-id="@item.MemberId">Detaylar</a>
                    <a class="btn btn-sm btn-warning " asp-action="Edit" asp-route-id="@item.MemberId">Düzenle</a>
                    <a class="btn btn-sm btn-danger " asp-action="Delete" asp-route-id="@item.MemberId"
                    asp-for="IsDeleted">Sil</a>
                </td>
                </td>
            </tr>

        }
    </tbody>
</table>

